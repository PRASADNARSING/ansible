- name: Install Packages
  hosts: local
  become: yes
  tasks:
    - name: Install packages
      community.general.packages:
        name: "{{ item }}"
        state: Installed
      loop:
      - nginx
      - gcc
      - git
      - zip
      - mysql